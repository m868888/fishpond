parameters:
  # for "merge" command
  data_to_append:
    require-dev:
      phpunit/phpunit: "^7.3"
      symplify/monorepo-builder: "^8.3"

  # for "split" command
  directories_to_repositories:
    packages/fishpond: "git@github.com:m868888/fishpond.git"
